# Required in production (Vercel). Your Render backend URL, no trailing slash.
# Example: https://wot-api.onrender.com
VITE_API_BASE_URL=

# Dev only — Vite proxy target (ignored in production builds).
# VITE_API_PROXY_TARGET=http://localhost:8080
